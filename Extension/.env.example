# KeyCare backend — never put OpenAI / LLM provider keys here.
# The Chrome extension is a client of the same API used by the Android app.

VITE_API_BASE_URL=http://localhost:8000
VITE_DEMO_MODE=true
